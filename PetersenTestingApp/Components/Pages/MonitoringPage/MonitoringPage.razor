@page "/monitoring"
@using PetersenTestingApp.Components.Monitoring

<div class="p-6 space-y-10">

    <!-- Header -->
    <h1 class="text-3xl font-bold">Pressure Monitoring Dashboard</h1>

    <!-- Sensor Grid -->
    <div class="grid gap-6 grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
        @foreach (var reading in liveReadings)
        {
            <LiveSensorCard Reading="reading" />
        }
    </div>

    <!-- Historical Chart Section -->
    <div class="mt-12 space-y-4">
        <h2 class="text-2xl font-bold">Historical Pressure Plot</h2>

        <EditForm Model="query" OnValidSubmit="OnQuerySubmit" class="grid grid-cols-1 sm:grid-cols-4 gap-4 items-end">
            <div>
                <label class="block text-sm font-medium text-gray-700">Sensor ID</label>
                <InputText @bind-Value="query.SensorId" class="form-input mt-1 block w-full" />
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700">Start Date</label>
                <InputDate @bind-Value="query.StartDate" class="form-input mt-1 block w-full" />
                <InputDate Type="InputDateType.Time" @bind-Value="query.StartDate" class="form-input mt-1 block w-full" />
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700">End Date</label>
                <InputDate @bind-Value="query.EndDate" class="form-input mt-1 block w-full" />
                <InputDate Type="InputDateType.Time" @bind-Value="query.EndDate" class="form-input mt-1 block w-full" />
            </div>
            <div>
                <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded w-full">Plot</button>
            </div>
        </EditForm>

        @if (plotData?.Any() == true)
        {
            <LineChart @ref="chart" TItem="double" Options="chartOptions" />
        }
    </div>
</div>
